# Android / Gradle build output
*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
app/build/
/captures
.externalNativeBuild
.cxx

# Catalog pipeline — cached HTML pages and virtualenv (large, regenerable)
tools/catalog_pipeline/cache/
tools/catalog_pipeline/venv/
scripts/__pycache__/

# Local agent / editor session artifacts — keep these OUT of the public repo
.claude/
.claude-sessions/
.githooks/
CLAUDE.md
